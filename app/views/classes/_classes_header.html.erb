<div class="navbar navbar-static-top" style = "border-bottom: #629b63; z-index: 0;">
  <div class = "navbar-inner">
    <div class = "container">
      <ul class = "nav pull-left">
        <li class="dropdown" id="menu1"><!-- Starts the "Arts" dropdown menu-->
          <a class="dropdown-toggle" data-toggle="dropdown" href="#">
            Math
            <b class="caret"></b>
          </a>
          <ul class="dropdown-menu" role="menu" aria-labelledby="dropdownMenu">
          <% if @math.present? %>
            <% @math.each do |a_class| %> 
              <li><a href="/classes/<%= a_class.slug %>"><%= a_class.name %></a></li>
            <% end %>
          <% else %>
             <li class=""><a href="#">Check back soon for content!</a></li>
          <% end %>
          </ul>
        </li><!--Ends dropdown menu-->
        <li class="dropdown" id="menu1"><!-- Starts the "Arts" dropdown menu-->
          <a class="dropdown-toggle" data-toggle="dropdown" href="#">
            Science
            <b class="caret"></b>
          </a>
          <ul class="dropdown-menu" role="menu" aria-labelledby="dropdownMenu">
            <% if @science.present? %>
              <% @science.each do |a_class| %> 
                <li><a href="/classes/<%= a_class.slug %>"><%= a_class.name %></a></li>
              <% end %>
            <% else %>
               <li class=""><a href="#">Check back soon for content!</a></li>
            <% end %>
          </ul>
        </li><!--Ends dropdown menu-->
        <li class="dropdown" id="menu1"><!-- Starts the "Arts" dropdown menu-->
          <a class="dropdown-toggle" data-toggle="dropdown" href="#">
            English
            <b class="caret"></b>
          </a>
          <ul class="dropdown-menu" role="menu" aria-labelledby="dropdownMenu">
           <% if @english.present? %>
              <% @english.each do |a_class| %> 
                <li><a href="/classes/<%= a_class.slug %>"><%= a_class.name %></a></li>
              <% end %>
            <% else %>
               <li class=""><a href="#">Check back soon for content!</a></li>
            <% end %>
          </ul>
        </li><!--Ends dropdown menu-->
         <li class="dropdown" id="menu1"><!-- Starts the "Arts" dropdown menu-->
          <a class="dropdown-toggle" data-toggle="dropdown" href="#">
            Social Studies
            <b class="caret"></b>
          </a>
          <ul class="dropdown-menu" role="menu" aria-labelledby="dropdownMenu">
            <% if @social_studies.present? %>
            <% @social_studies.each do |a_class| %> 
                <li><a href="/classes/<%= a_class.slug %>"><%= a_class.name %></a></li>
              <% end %>
            <% else %>
               <li class=""><a href="#">Check back soon for content!</a></li>
            <% end %>
          </ul>
        </li><!--Ends dropdown menu-->
        <li class="dropdown" id="menu1"><!-- Starts the "Arts" dropdown menu-->
          <a class="dropdown-toggle" data-toggle="dropdown" href="#">
            Foreign Language
            <b class="caret"></b>
          </a>
          <ul class="dropdown-menu" role="menu" aria-labelledby="dropdownMenu">
            <% if @foreign_language.present? %>
            <% @foreign_language.each do |a_class| %> 
                <li><a href="/classes/<%= a_class.slug %>"><%= a_class.name %></a></li>
              <% end %>
            <% else %>
               <li class=""><a href="#">Check back soon for content!</a></li>
            <% end %>
          </ul>
        </li><!--Ends dropdown menu-->
        <li class="dropdown" id="menu1"><!-- Starts the "Arts" dropdown menu-->
          <a class="dropdown-toggle" data-toggle="dropdown" href="#">
            Arts and Music
            <b class="caret"></b>
          </a>
          <ul class="dropdown-menu" role="menu" aria-labelledby="dropdownMenu">
            <% if @arts_and_music.present? %>
            <% @arts_and_music.each do |a_class| %> 
                <li><a href="/classes/<%= a_class.slug %>"><%= a_class.name %></a></li>
              <% end %>
            <% else %>
               <li class=""><a href="#">Check back soon for content!</a></li>
            <% end %>
          </ul>
        </li><!--Ends dropdown menu-->
        <li class="dropdown" id="menu1"><!-- Starts the "Arts" dropdown menu-->
          <a class="dropdown-toggle" data-toggle="dropdown" href="#">
            Arts and Music
            <b class="caret"></b>
          </a>
          <ul class="dropdown-menu" role="menu" aria-labelledby="dropdownMenu">
            <% if @grades.present? %>
            <% @grades.each do |a_class| %> 
                <li><a href="/classes/<%= a_class.slug %>"><%= a_class.name %></a></li>
              <% end %>
            <% else %>
               <li class=""><a href="#">Check back soon for content!</a></li>
            <% end %>
          </ul>
        </li><!--Ends dropdown menu-->

      </ul>
    </div>
  </div>
</div>