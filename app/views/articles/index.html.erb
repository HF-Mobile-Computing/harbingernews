<script type="text/javascript">

$(function() {
  // Add "active" to nav bar
  $('#nav_news').addClass('active');

});

</script>

<div class="container" id="articleview">
	
	<!--<table class="table">
				<thead>
					<tr>
						<th>Title</th>
						<th>Author</th>
						<th>Description</th>
						<th>Date Created</th>
					</tr>
				</thead>
				<tbody>
				  <% @articles.each do |article| %>
				  	<tr>
					  	<td><%= link_to article.title, article %></td>
						<td><%= article.author %></td>
						<td><%= article.description %></td>
						<td><%= article.created_at.strftime("%m/%d/%Y") %></td>
					</tr>
				  <% end %>
				</tbody>
	</table>-->
	<div class="span6 offset3">
		<h1 style="text-align:center;">Tornado News Articles</h1>
		<% @articles.each do |article| %>
			<div class="well">
			    <h2 style="text-align: center;">
			      <%= article.title %>
			    </h2>
			    <h4>By <%= article.author %><div style="text-align: right;">Published <%= article.created_at.strftime('%B %d, %Y')%></div></h4>
			      <%=raw article.body %>
			</div>
		<% end %>
  	</div>
</div>
