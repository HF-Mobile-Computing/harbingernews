<%= form_for(@recent_event) do |f| %>
  <% if @recent_event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@recent_event.errors.count, "error") %> prohibited this recent_event from being saved:</h2>

      <ul>
      <% @recent_event.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
